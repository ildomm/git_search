$("#counter").html("<%= @counter %>");
$("#repositories").html("<%= escape_javascript(render partial: 'search',
                                                      locals: { repositories: @repositories, term: @term } ) %>");
